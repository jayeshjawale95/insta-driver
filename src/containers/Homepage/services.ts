import driverData from "../../data/drivers";
import driver1Chat from "../../data/driver1_chat";

export const fetchDrivers = () => {
  return driverData;
};

export const fetchDriver1Chat = () => {
  return driver1Chat;
}
